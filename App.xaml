<Application x:Class="NRftWManagerUI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:NRftWManagerUI"
             xmlns:system="clr-namespace:System.Windows;assembly=PresentationFramework"
             StartupUri="DemoWindow.xaml">
  <Application.Resources>
    <System:Double xmlns:System="clr-namespace:System;assembly=System.Runtime" x:Key="{x:Static system:SystemParameters.VerticalScrollBarWidthKey}">7</System:Double>
    <System:Double xmlns:System="clr-namespace:System;assembly=System.Runtime" x:Key="{x:Static system:SystemParameters.HorizontalScrollBarHeightKey}">7</System:Double>
    <Color x:Key="VoidColor">#020610</Color>
    <Color x:Key="SurfaceColor">#07101F</Color>
    <Color x:Key="SurfaceRaisedColor">#0A1628</Color>
    <Color x:Key="LineColor">#183C68</Color>
    <Color x:Key="CyanColor">#7B39FF</Color>
    <Color x:Key="CyanSoftColor">#E7DAFF</Color>
    <Color x:Key="AmberColor">#FFBF5F</Color>
    <Color x:Key="PurpleColor">#C653FF</Color>

    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource VoidColor}" />
    <SolidColorBrush x:Key="PanelBrush" Color="{StaticResource SurfaceColor}" />
    <SolidColorBrush x:Key="PanelAltBrush" Color="{StaticResource SurfaceRaisedColor}" />
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource LineColor}" />
    <SolidColorBrush x:Key="MutedBrush" Color="#7890AF" />
    <SolidColorBrush x:Key="TextBrush" Color="#EEF5FF" />
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource CyanColor}" />
    <SolidColorBrush x:Key="AccentDarkBrush" Color="#5B45B0" />
    <SolidColorBrush x:Key="NodeBrush" Color="#20D7FF" />
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource AmberColor}" />
    <SolidColorBrush x:Key="PurpleBrush" Color="{StaticResource PurpleColor}" />

    <LinearGradientBrush x:Key="SciFiGridBrush" StartPoint="0,0" EndPoint="1,1">
      <GradientStop Color="#030916" Offset="0" />
      <GradientStop Color="#01040C" Offset="0.58" />
      <GradientStop Color="#07031A" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="PanelGradient" StartPoint="0,0" EndPoint="1,1">
      <GradientStop Color="#F50A1425" Offset="0" />
      <GradientStop Color="#F5050B16" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="HeaderGradient" StartPoint="0,0" EndPoint="1,0">
      <GradientStop Color="#FF071222" Offset="0" />
      <GradientStop Color="#FF030812" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="ActionGradient" StartPoint="0,0" EndPoint="1,1">
      <GradientStop Color="#913CFF" Offset="0" />
      <GradientStop Color="#5922D8" Offset="0.62" />
      <GradientStop Color="#244CFF" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="SecondaryGradient" StartPoint="0,0" EndPoint="0,1">
      <GradientStop Color="#0D1A2E" Offset="0" />
      <GradientStop Color="#071221" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="ControlCellGradient" StartPoint="0,0" EndPoint="1,1">
      <GradientStop Color="#0D1A2E" Offset="0" />
      <GradientStop Color="#07111F" Offset="1" />
    </LinearGradientBrush>

    <DrawingBrush x:Key="TechGridBrush" TileMode="Tile" Viewport="0,0,34,34" ViewportUnits="Absolute" Opacity="0.2">
      <DrawingBrush.Drawing>
        <GeometryDrawing Brush="Transparent">
          <GeometryDrawing.Pen><Pen Brush="#1B4D80" Thickness="0.55" /></GeometryDrawing.Pen>
          <GeometryDrawing.Geometry><GeometryGroup><LineGeometry StartPoint="0,0" EndPoint="34,0" /><LineGeometry StartPoint="0,0" EndPoint="0,34" /></GeometryGroup></GeometryDrawing.Geometry>
        </GeometryDrawing>
      </DrawingBrush.Drawing>
    </DrawingBrush>

    <LinearGradientBrush x:Key="HeroShadeBrush" StartPoint="0,0" EndPoint="1,0">
      <GradientStop Color="#F2030812" Offset="0" />
      <GradientStop Color="#91030A18" Offset="0.4" />
      <GradientStop Color="#4A09051A" Offset="0.72" />
      <GradientStop Color="#E9030812" Offset="1" />
    </LinearGradientBrush>

    <ImageBrush x:Key="AtlasInputShellBrush" ImageSource="Assets/ui-input-shell.png" Stretch="Fill" />

    <Style TargetType="TextBlock">
      <Setter Property="FontFamily" Value="Segoe UI Variable Display, Segoe UI" />
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    </Style>

    <Style x:Key="DisplayTitle" TargetType="TextBlock">
      <Setter Property="FontFamily" Value="Segoe UI Variable Display, Segoe UI" />
      <Setter Property="FontWeight" Value="Light" />
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    </Style>

    <Style x:Key="SectionTitle" TargetType="TextBlock" BasedOn="{StaticResource DisplayTitle}">
      <Setter Property="FontSize" Value="21" />
      <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="MicroLabel" TargetType="TextBlock">
      <Setter Property="FontFamily" Value="Bahnschrift, Segoe UI" />
      <Setter Property="FontSize" Value="10" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
      <Setter Property="Visibility" Value="Collapsed" />
    </Style>

    <Style TargetType="TextBox">
      <Setter Property="FontFamily" Value="Consolas" />
      <Setter Property="Background" Value="#E8040B17" />
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
      <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}" />
      <Setter Property="SelectionBrush" Value="#809D5CFF" />
      <Setter Property="BorderBrush" Value="#234D7F" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TextBox">
            <Border x:Name="InputShell" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
              <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="InputShell" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.42" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="ComboBox">
      <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
      <Setter Property="FontSize" Value="12" />
      <Setter Property="Background" Value="#060D19" />
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
      <Setter Property="BorderBrush" Value="#234D7F" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ComboBox">
            <Grid>
              <ToggleButton x:Name="Toggle" IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" Focusable="False" ClickMode="Press" Background="Transparent" BorderThickness="0">
                <ToggleButton.Template>
                  <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="ComboShell" Background="{Binding Background, RelativeSource={RelativeSource AncestorType=ComboBox}}" BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=ComboBox}}" BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource AncestorType=ComboBox}}" CornerRadius="8">
                      <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="34" /></Grid.ColumnDefinitions>
                        <ContentPresenter Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}" ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType=ComboBox}}" Margin="{Binding Padding, RelativeSource={RelativeSource AncestorType=ComboBox}}" VerticalAlignment="Center" TextElement.FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType=ComboBox}}" TextElement.FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=ComboBox}}" TextElement.Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ComboBox}}" />
                        <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ComboBox}}" StrokeThickness="1.5" HorizontalAlignment="Center" VerticalAlignment="Center" />
                      </Grid>
                    </Border>
                  </ControlTemplate>
                </ToggleButton.Template>
              </ToggleButton>
              <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                <Border Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" MaxHeight="{TemplateBinding MaxDropDownHeight}" Background="#07111F" BorderBrush="#315F98" BorderThickness="1" CornerRadius="5" Margin="0,5,0,0" Padding="5">
                  <ScrollViewer CanContentScroll="True" VerticalScrollBarVisibility="Auto"><ItemsPresenter /></ScrollViewer>
                </Border>
              </Popup>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" /></Trigger>
              <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.42" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
      <Setter Property="FontSize" Value="12" />
      <Setter Property="Padding" Value="11,9" />
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ComboBoxItem">
            <Border x:Name="ItemShell" Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="1" CornerRadius="6" Padding="{TemplateBinding Padding}">
              <ContentPresenter VerticalAlignment="Center" TextElement.Foreground="{TemplateBinding Foreground}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ItemShell" Property="Background" Value="#10213A" /></Trigger>
              <Trigger Property="IsSelected" Value="True"><Setter TargetName="ItemShell" Property="Background" Value="#29145A" /><Setter TargetName="ItemShell" Property="BorderBrush" Value="#8544FF" /><Setter Property="Foreground" Value="#FFFFFF" /></Trigger>
              <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.4" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="CheckBox">
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
      <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="CheckBox">
            <StackPanel Orientation="Horizontal">
              <Border x:Name="Track" Width="38" Height="21" CornerRadius="11" Background="#14223A" BorderBrush="#294B78" BorderThickness="1" VerticalAlignment="Center">
                <Ellipse x:Name="Knob" Width="10" Height="10" Fill="#697185" HorizontalAlignment="Left" Margin="4,0" />
              </Border>
              <ContentPresenter Margin="9,0,0,0" VerticalAlignment="Center" />
            </StackPanel>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Track" Property="Background" Value="#6D2EF2" />
                <Setter TargetName="Track" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                <Setter TargetName="Knob" Property="Fill" Value="#FFFFFF" />
                <Setter TargetName="Knob" Property="HorizontalAlignment" Value="Right" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.4" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="ListBox">
      <Setter Property="Background" Value="#B9030914" />
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
      <Setter Property="BorderBrush" Value="#183C68" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="7" />
    </Style>

    <Style TargetType="ListBoxItem">
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Padding" Value="9,6" />
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="#5A351773" />
          <Setter Property="Foreground" Value="#FFFFFF" />
        </Trigger>
      </Style.Triggers>
    </Style>

    <Style x:Key="Card" TargetType="Border">
      <Setter Property="Background" Value="{StaticResource PanelGradient}" />
      <Setter Property="BorderBrush" Value="#183C68" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="6" />
      <Setter Property="Padding" Value="24" />
      <Setter Property="SnapsToDevicePixels" Value="True" />
      <Setter Property="Effect"><Setter.Value><DropShadowEffect Color="#154F9A" BlurRadius="12" ShadowDepth="0" Opacity="0.2" /></Setter.Value></Setter>
    </Style>

    <Style x:Key="AngularPanel" TargetType="Border" BasedOn="{StaticResource Card}">
      <Setter Property="Padding" Value="24" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="0" />
      <Setter Property="BorderBrush" Value="#1E548F" />
      <Setter Property="Background" Value="#E6071020" />
      <Setter Property="Effect"><Setter.Value><DropShadowEffect Color="#1F5CBD" BlurRadius="16" ShadowDepth="0" Opacity="0.22" /></Setter.Value></Setter>
    </Style>

    <Style x:Key="CutCornerPanelStyle" TargetType="local:CutCornerPanel">
      <Setter Property="Padding" Value="24" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="BorderBrush" Value="#1E548F" />
      <Setter Property="Background" Value="#E6071020" />
      <Setter Property="Effect"><Setter.Value><DropShadowEffect Color="#1F5CBD" BlurRadius="10" ShadowDepth="0" Opacity="0.18" /></Setter.Value></Setter>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="local:CutCornerPanel">
            <Grid SnapsToDevicePixels="True">
              <Border x:Name="Surface" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" />
              <Border Margin="6" BorderBrush="#214F8E3D" BorderThickness="1" IsHitTestVisible="False" />
              <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" Margin="{TemplateBinding Padding}" />
              <Grid IsHitTestVisible="False">
                <Grid.RowDefinitions><RowDefinition Height="18" /><RowDefinition Height="*" /><RowDefinition Height="18" /></Grid.RowDefinitions>
                <Grid.ColumnDefinitions><ColumnDefinition Width="18" /><ColumnDefinition Width="*" /><ColumnDefinition Width="18" /></Grid.ColumnDefinitions>
                <Image Source="Assets/ui-frame-tl.png" Grid.Row="0" Grid.Column="0" Stretch="None" HorizontalAlignment="Left" VerticalAlignment="Top" />
                <Image Source="Assets/ui-frame-t.png" Grid.Row="0" Grid.Column="1" Stretch="Fill" VerticalAlignment="Top" Height="4" />
                <Image Source="Assets/ui-frame-tr.png" Grid.Row="0" Grid.Column="2" Stretch="None" HorizontalAlignment="Right" VerticalAlignment="Top" />
                <Image Source="Assets/ui-frame-l.png" Grid.Row="1" Grid.Column="0" Stretch="Fill" HorizontalAlignment="Left" Width="4" />
                <Image Source="Assets/ui-frame-r.png" Grid.Row="1" Grid.Column="2" Stretch="Fill" HorizontalAlignment="Right" Width="4" />
                <Image Source="Assets/ui-frame-bl.png" Grid.Row="2" Grid.Column="0" Stretch="None" HorizontalAlignment="Left" VerticalAlignment="Bottom" />
                <Image Source="Assets/ui-frame-b.png" Grid.Row="2" Grid.Column="1" Stretch="Fill" VerticalAlignment="Bottom" Height="4" />
                <Image Source="Assets/ui-frame-br.png" Grid.Row="2" Grid.Column="2" Stretch="None" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
              </Grid>
              <Canvas IsHitTestVisible="False" Visibility="Collapsed">
                <Polygon Points="0,0 14,0 0,14" Fill="#020711" />
                <Path Data="M0,14 L14,0 L36,0" Stroke="#54C7FF" StrokeThickness="1" />
                <Path Data="M0,14 L14,0" Stroke="#B058FF" StrokeThickness="1.8" Opacity="0.62" />
                <Path Data="M0,14 L0,36" Stroke="#54C7FF" StrokeThickness="1" />
              </Canvas>
              <Canvas IsHitTestVisible="False" HorizontalAlignment="Right" Visibility="Collapsed">
                <Polygon Points="0,0 14,0 14,14" Fill="#020711" />
                <Path Data="M0,0 L14,14 L14,36" Stroke="#54C7FF" StrokeThickness="1" />
                <Path Data="M0,0 L14,14" Stroke="#B058FF" StrokeThickness="1.8" Opacity="0.62" />
              </Canvas>
              <Canvas IsHitTestVisible="False" VerticalAlignment="Bottom" Visibility="Collapsed">
                <Polygon Points="0,14 14,0 0,0" Fill="#020711" />
                <Path Data="M0,0 L14,14 L36,14" Stroke="#54C7FF" StrokeThickness="1" />
                <Path Data="M0,0 L14,14" Stroke="#B058FF" StrokeThickness="1.8" Opacity="0.62" />
              </Canvas>
              <Canvas IsHitTestVisible="False" HorizontalAlignment="Right" VerticalAlignment="Bottom" Visibility="Collapsed">
                <Polygon Points="14,14 0,14 14,0" Fill="#020711" />
                <Path Data="M0,14 L14,0 L14,-22" Stroke="#54C7FF" StrokeThickness="1" />
                <Path Data="M0,14 L14,0" Stroke="#B058FF" StrokeThickness="1.8" Opacity="0.62" />
              </Canvas>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="AtlasPanelFrameStyle" TargetType="local:CutCornerPanel" BasedOn="{StaticResource CutCornerPanelStyle}">
      <Setter Property="Padding" Value="18" />
      <Setter Property="Background" Value="#D9050B18" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="local:CutCornerPanel">
            <Grid SnapsToDevicePixels="True">
              <Image Source="Assets/ui-frame-panel-wide.png" Stretch="Fill" Opacity="0.94" IsHitTestVisible="False" />
              <Border Background="{TemplateBinding Background}" Margin="5" />
              <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" Margin="{TemplateBinding Padding}" />
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="SidebarMutedEntry" TargetType="Border">
      <Setter Property="Padding" Value="18,10" />
      <Setter Property="Margin" Value="0,0,0,4" />
      <Setter Property="Opacity" Value="0.64" />
      <Setter Property="Cursor" Value="Arrow" />
    </Style>

    <Style x:Key="HeaderIconButton" TargetType="Button">
      <Setter Property="Width" Value="34" />
      <Setter Property="Height" Value="34" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="Foreground" Value="#B9D0F2" />
      <Setter Property="Background" Value="#091526" />
      <Setter Property="BorderBrush" Value="#245381" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="IconShell" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="IconShell" Property="BorderBrush" Value="#A35BFF" /><Setter TargetName="IconShell" Property="Background" Value="#16113A" /></Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter TargetName="IconShell" Property="Background" Value="#24184A" /></Trigger>
              <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.4" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="HeroButton" TargetType="Button">
      <Setter Property="Padding" Value="18,8" />
      <Setter Property="MinWidth" Value="150" />
      <Setter Property="BorderBrush" Value="#B05AFF" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Background" Value="#30124F" />
      <Setter Property="Foreground" Value="#F6F2FF" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Grid>
              <Border x:Name="HeroShell" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" />
              <Border BorderBrush="#783DE0" BorderThickness="1" Margin="3" IsHitTestVisible="False" />
              <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center" TextElement.Foreground="{TemplateBinding Foreground}" />
              <Canvas IsHitTestVisible="False" HorizontalAlignment="Right"><Path Data="M0,0 L9,9 L9,20" Stroke="#C66DFF" StrokeThickness="1" /></Canvas>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="HeroShell" Property="Background" Value="#54218C" /><Setter TargetName="HeroShell" Property="Effect"><Setter.Value><DropShadowEffect Color="#883CFF" BlurRadius="16" ShadowDepth="0" Opacity="0.7" /></Setter.Value></Setter></Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter TargetName="HeroShell" Property="Background" Value="#271045" /></Trigger>
              <Trigger Property="IsEnabled" Value="False"><Setter TargetName="HeroShell" Property="Background" Value="#18202D" /><Setter Property="Foreground" Value="#748095" /><Setter Property="BorderBrush" Value="#34445B" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="LibraryFilterButton" TargetType="Button">
      <Setter Property="Padding" Value="18,6" />
      <Setter Property="Margin" Value="7,0,0,0" />
      <Setter Property="FontSize" Value="11" />
      <Setter Property="Background" Value="#0A1728" />
      <Setter Property="Foreground" Value="#C4D6EE" />
      <Setter Property="BorderBrush" Value="#244B78" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="FilterShell" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextElement.Foreground="{TemplateBinding Foreground}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="FilterShell" Property="BorderBrush" Value="#9F5AFF" /></Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter TargetName="FilterShell" Property="Background" Value="#35146C" /></Trigger>
              <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.45" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="GameCardButton" TargetType="Button">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="Transparent" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Grid>
              <Border x:Name="CardHost" Background="#0A1322" BorderBrush="#24568F" BorderThickness="1" />
              <ContentPresenter />
              <Grid IsHitTestVisible="False">
                <Grid.RowDefinitions><RowDefinition Height="18" /><RowDefinition Height="*" /><RowDefinition Height="18" /></Grid.RowDefinitions>
                <Grid.ColumnDefinitions><ColumnDefinition Width="18" /><ColumnDefinition Width="*" /><ColumnDefinition Width="18" /></Grid.ColumnDefinitions>
                <Image Source="Assets/ui-frame-tl.png" Grid.Row="0" Grid.Column="0" Stretch="None" HorizontalAlignment="Left" VerticalAlignment="Top" />
                <Image Source="Assets/ui-frame-t.png" Grid.Row="0" Grid.Column="1" Stretch="Fill" VerticalAlignment="Top" Height="4" />
                <Image Source="Assets/ui-frame-tr.png" Grid.Row="0" Grid.Column="2" Stretch="None" HorizontalAlignment="Right" VerticalAlignment="Top" />
                <Image Source="Assets/ui-frame-l.png" Grid.Row="1" Grid.Column="0" Stretch="Fill" HorizontalAlignment="Left" Width="4" />
                <Image Source="Assets/ui-frame-r.png" Grid.Row="1" Grid.Column="2" Stretch="Fill" HorizontalAlignment="Right" Width="4" />
                <Image Source="Assets/ui-frame-bl.png" Grid.Row="2" Grid.Column="0" Stretch="None" HorizontalAlignment="Left" VerticalAlignment="Bottom" />
                <Image Source="Assets/ui-frame-b.png" Grid.Row="2" Grid.Column="1" Stretch="Fill" VerticalAlignment="Bottom" Height="4" />
                <Image Source="Assets/ui-frame-br.png" Grid.Row="2" Grid.Column="2" Stretch="None" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
              </Grid>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="CardHost" Property="BorderBrush" Value="#A259FF" /><Setter TargetName="CardHost" Property="Effect"><Setter.Value><DropShadowEffect Color="#7430E8" BlurRadius="20" ShadowDepth="0" Opacity="0.52" /></Setter.Value></Setter></Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter TargetName="CardHost" Property="Opacity" Value="0.78" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="RailTitle" TargetType="TextBlock">
      <Setter Property="FontFamily" Value="Bahnschrift, Segoe UI" />
      <Setter Property="FontSize" Value="16" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Foreground" Value="#9DD8FF" />
    </Style>

    <Style x:Key="LinkRow" TargetType="Border">
      <Setter Property="Background" Value="#080F1D" />
      <Setter Property="BorderBrush" Value="#1A3F6B" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="4" />
      <Setter Property="Padding" Value="9,6" />
      <Setter Property="Margin" Value="0,4,0,0" />
    </Style>

    <Style x:Key="ActivityRow" TargetType="Border">
      <Setter Property="Background" Value="#070E1B" />
      <Setter Property="BorderBrush" Value="#183A63" />
      <Setter Property="BorderThickness" Value="0,0,0,1" />
      <Setter Property="Padding" Value="8,10" />
    </Style>

    <Style x:Key="QuickFeatureRow" TargetType="Border">
      <Setter Property="Background" Value="#0A1323" />
      <Setter Property="BorderBrush" Value="#1B426F" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="4" />
      <Setter Property="Padding" Value="11,10" />
      <Setter Property="Margin" Value="0,7,0,0" />
    </Style>

    <Style x:Key="TelemetryCard" TargetType="Border" BasedOn="{StaticResource Card}">
      <Setter Property="Padding" Value="13,11" />
      <Setter Property="Background" Value="#E00C121A" />
      <Setter Property="BorderBrush" Value="#1E2A35" />
    </Style>

    <Style x:Key="ActionButton" TargetType="Button">
      <Setter Property="Background" Value="{StaticResource ActionGradient}" />
      <Setter Property="Foreground" Value="#FFFFFF" />
      <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="18,10" />
      <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Grid>
              <Border x:Name="Glow" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5" />
              <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center" TextElement.Foreground="{TemplateBinding Foreground}" />
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Glow" Property="Background" Value="#A550FF" />
                <Setter TargetName="Glow" Property="Effect"><Setter.Value><DropShadowEffect Color="#7B39FF" BlurRadius="16" ShadowDepth="0" Opacity="0.55" /></Setter.Value></Setter>
              </Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter TargetName="Glow" Property="Background" Value="#7465E8" /></Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="#686A74" />
                <Setter Property="BorderBrush" Value="#282A32" />
                <Setter TargetName="Glow" Property="Background" Value="#17181D" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
      <Setter Property="Background" Value="{StaticResource SecondaryGradient}" />
      <Setter Property="BorderBrush" Value="#21466F" />
      <Setter Property="Foreground" Value="#E5F0FF" />
    </Style>

    <Style x:Key="WindowControlButton" TargetType="Button">
      <Setter Property="Width" Value="38" />
      <Setter Property="Height" Value="32" />
      <Setter Property="Margin" Value="4,0,0,0" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="Transparent" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Foreground" Value="#A8ADBA" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="ControlShell" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#242630" /><Setter Property="Foreground" Value="#FFFFFF" /></Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter Property="Background" Value="#34313F" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="CloseWindowButton" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#C83B4A" /><Setter Property="Foreground" Value="#FFFFFF" /></Trigger>
        <Trigger Property="IsPressed" Value="True"><Setter Property="Background" Value="#9E2938" /></Trigger>
      </Style.Triggers>
    </Style>

    <Style x:Key="RarityButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
      <Setter Property="MinWidth" Value="104" />
      <Setter Property="Padding" Value="12,8" />
    </Style>

    <Style x:Key="SidebarGlyph" TargetType="TextBlock">
      <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
      <Setter Property="FontSize" Value="19" />
      <Setter Property="Foreground" Value="#62CFFF" />
      <Setter Property="Width" Value="35" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="SidebarCaption" TargetType="TextBlock">
      <Setter Property="FontSize" Value="9" />
      <Setter Property="Foreground" Value="#607A9D" />
      <Setter Property="Margin" Value="0,3,0,0" />
    </Style>

    <Style x:Key="SidebarNavigationButton" TargetType="RadioButton">
      <Setter Property="Foreground" Value="#A8BAD2" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="Transparent" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="17,9" />
      <Setter Property="Margin" Value="0,0,0,7" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="GroupName" Value="MainNavigation" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="RadioButton">
            <Grid>
              <Border x:Name="NavShell" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                <Grid><Border x:Name="ActiveLine" Width="2" Background="#8D45FF" HorizontalAlignment="Left" Margin="-14,-6,0,-6" Visibility="Collapsed" /><ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}" /></Grid>
              </Border>
              <Canvas IsHitTestVisible="False" HorizontalAlignment="Right" VerticalAlignment="Top" Visibility="Collapsed" x:Name="NavCorner"><Path Data="M0,0 L12,12 L12,30" Stroke="#B05AFF" StrokeThickness="1" /></Canvas>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="NavShell" Property="Background" Value="#0A1930" /><Setter Property="Foreground" Value="#FFFFFF" /></Trigger>
              <Trigger Property="IsChecked" Value="True"><Setter TargetName="NavShell" Property="Background" Value="#251044" /><Setter TargetName="NavShell" Property="BorderBrush" Value="#8A42F4" /><Setter TargetName="NavShell" Property="Effect"><Setter.Value><DropShadowEffect Color="#7B39FF" BlurRadius="14" ShadowDepth="0" Opacity="0.42" /></Setter.Value></Setter><Setter TargetName="ActiveLine" Property="Visibility" Value="Visible" /><Setter TargetName="NavCorner" Property="Visibility" Value="Visible" /><Setter Property="Foreground" Value="#FFFFFF" /></Trigger>
              <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.38" /><Setter Property="Cursor" Value="Arrow" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="SidebarTabControl" TargetType="TabControl">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TabControl">
            <ContentPresenter ContentSource="SelectedContent" />
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="TabControl">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TabControl">
            <Grid>
              <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
              <TabPanel IsItemsHost="True" Background="Transparent" />
              <Border Grid.Row="1" Background="Transparent" BorderThickness="0" Margin="0,14,0,0">
                <ContentPresenter ContentSource="SelectedContent" />
              </Border>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="TabItem">
      <Setter Property="Foreground" Value="#9AA4B5" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Padding" Value="18,10" />
      <Setter Property="Margin" Value="0,0,18,0" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TabItem">
            <Grid>
              <Border x:Name="TabShell" Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="0" Padding="{TemplateBinding Padding}">
                <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" />
              </Border>
              <Border x:Name="EnergyLine" Height="2" Background="{StaticResource AccentBrush}" VerticalAlignment="Bottom" Margin="18,0" Visibility="Collapsed" />
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsSelected" Value="True">
                <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                <Setter TargetName="TabShell" Property="Background" Value="Transparent" />
                <Setter TargetName="EnergyLine" Property="Visibility" Value="Visible" />
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True"><Setter Property="Foreground" Value="#D7E6F5" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="ScrollBar">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Width" Value="7" />
      <Setter Property="MinWidth" Value="0" />
      <Setter Property="Opacity" Value="0.42" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ScrollBar">
            <Grid Background="{TemplateBinding Background}">
              <Track x:Name="PART_Track" Orientation="{TemplateBinding Orientation}" IsDirectionReversed="True">
                <Track.DecreaseRepeatButton><RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" /></Track.DecreaseRepeatButton>
                <Track.Thumb><Thumb Background="#9D5CFF"><Thumb.Template><ControlTemplate TargetType="Thumb"><Border Background="{TemplateBinding Background}" CornerRadius="2" Margin="1,4" /></ControlTemplate></Thumb.Template></Thumb></Track.Thumb>
                <Track.IncreaseRepeatButton><RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" /></Track.IncreaseRepeatButton>
              </Track>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True"><Setter Property="Opacity" Value="0.92" /></Trigger>
        <Trigger Property="Orientation" Value="Horizontal"><Setter Property="Width" Value="Auto" /><Setter Property="Height" Value="7" /></Trigger>
      </Style.Triggers>
    </Style>
  </Application.Resources>
</Application>
